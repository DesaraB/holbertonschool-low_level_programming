#include "main.h"
#include <stdio.h>

/**
* _strlen - returns the length of a string
* @s: points to the sting
* Return: value
*/
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
